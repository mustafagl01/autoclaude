"# autoclaude

UK Takeaway Phone Order Assistant Dashboard (Next.js).

## Vercel deploy

**Root Directory:** In Vercel project settings (Settings → General), set **Root Directory** to `dashboard-app`. The Next.js app and `package.json` are in that folder." 
